# Small-Lifeform-Detection-Agent
Designed a custom YOLO architecture optimized for small-object recognition (&lt;10 cm targets within a 2 m range), significantly improving precision over standard YOLO variants. • Fused FasterRCNN and the custom YOLO model into a single agent trained on high-purity datasets, enabling reliable detection at scales where existing detectors fail.
